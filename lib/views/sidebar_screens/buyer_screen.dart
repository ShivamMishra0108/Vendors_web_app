import 'package:flutter/material.dart';

class BuyerScreen extends StatelessWidget {
    static const id = '\buyerScreen';

  const BuyerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Buyer"),);
  }
}